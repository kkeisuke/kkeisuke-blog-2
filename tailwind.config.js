const colors = require('tailwindcss/colors')

module.exports = {
  content: [],
  theme: {
    colors: {
      transparent: 'transparent',
      current: 'currentColor',
      gray: colors.stone,
      blue: colors.sky
    },
    extend: {}
  },
  plugins: []
}
